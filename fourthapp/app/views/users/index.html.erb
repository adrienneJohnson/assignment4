<ul>
<% @users.each do |user| %>
  <li><span><%= link_to user.name, user_path(user) %></span> 
    <span><%= link_to "Delete User", user_path(user), method: :delete, data: {confirm: "Are you sure?"} %></span></li>
<% end %>

</ul>


